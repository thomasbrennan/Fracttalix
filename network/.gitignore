# DDN runtime state (regenerated by health monitor)
.network-state.json
.health-log.jsonl

# Snapshot archives (large binary artifacts)
snapshots/*.tar.gz

# Keep snapshot manifests
!snapshots/*.manifest.json
